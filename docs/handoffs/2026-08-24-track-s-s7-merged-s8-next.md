---
handoff_id: 2026-08-24-track-s-s7-merged-s8-next
supersedes: 2026-08-24-track-s-s7-review-repaired
handoff_status: superseded
history: HISTORY#604
---

# Track S S7 published — S8 next

## Protected publication state

- Track S S7 is protected-main behavior through merged PR #226 at merge commit
  `338534361429103a00f2bd19f0dae14eee4b54c6`.
- Exact source head `d50315a4964f2b00571907fd6e6675a2459ce56b`
  was signed, matched the PR head, and passed `repo-hygiene`,
  `chroma-real-smoke`, `locomo-quickstart-bil2`, `test-and-benchmark`,
  `pgvector-integration`, `package-smoke`, and `registry-plan` before merge.
- The exact-head GitHub Codex review completed without a listed finding.
  CodeRabbit CLI's workspace-status concern was rejected against the governing
  mutable-status/append-only-history boundary and is documented in PR comment
  5402099713.
- The merged S7 remote branch was deleted by repository policy and pruned. Its
  fully merged local branch was deleted. This bounded handoff branch starts
  exactly at protected `origin/main@3385343`.

## S7 proof boundary

- Exact repeated-mention ENT evidence, scoped and explicit identity,
  functional/multivalued temporal reconciliation, concurrent replay
  idempotency, as-of intervals, cross-boundary REL refusal, and closed
  predicate admission are published.
- The provider-free retrieved-ENT conformance fixture resolves 5/5 entities
  through complete exact SPAN-to-RAW chains.
- Historical native LoCoMo ENT coverage remains 0.0000. Native-corpus freeze,
  independent review, scorer eligibility, full-corpus measurement, and graph
  promotion remain S9 work; S7 publication does not imply them.

## Next work: S8 retrieval coherence

Begin S8 only from the verified protected-main successor containing this
handoff. Read `SEAM_SPEC_V0.1.md`, `docs/MIRL_V1.md`, and the S8 section of
`docs/roadmap/MEMORY_GUARANTEES_CAMPAIGN.md` before product changes.

S8 must satisfy every campaign exit condition:

- a legacy-policy plan executes only the legacy adapter;
- every shipped surface returns the same candidate IDs and order as direct
  `SeamRuntime.retrieve()` under the same request;
- absent, all-1, zero, and non-unit weights replay exactly, with all-1 bitwise
  identical to `reciprocal-rank-fusion/2`;
- misspelled or unknown leg names fail before search;
- every successful enabled retrieval records exactly one tenant-scoped event,
  while telemetry failure cannot alter the answer; and
- accepted identity merges are reversible and fully audited.

The deployment-readiness audit also assigns S8 the bounded policy-resolution,
process-lifetime flag-cache, SQLite 999-variable-floor, boundary-only SQL, and
legacy-versus-RRF decisions. S9 remains the promotion gate; do not turn S8 into
a new benchmark headline or enable unqualified graph/scorer behavior.

## Preserve these boundaries

- The dirty primary checkout, unrelated worktrees, sibling repositories,
  ignored artifacts, and operator assets remain outside this workstream.
- PRs #207, #213, and #221 remain separate older workstreams and are not
  qualified by S7 publication.
- Start S8 on a new branch from the protected head containing this handoff.
  Do not continue implementation on the documentation-only handoff branch.
